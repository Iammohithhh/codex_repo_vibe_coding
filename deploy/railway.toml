[build]
builder = "dockerfile"

[deploy]
restartPolicy = "on_failure"
healthchecks = {enabled = true}

[env]
ENVIRONMENT = "production"
PYTHONUNBUFFERED = "1"

[docs]
name = "Paper2Product 2.0"
description = "AI Research OS — Python API + SPA frontend on a single port"
